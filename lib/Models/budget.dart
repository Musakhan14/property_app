class Budget {
  final String id;
  final String bTitle;

  Budget({
    required this.id,
    required this.bTitle,
  });
  // : assert(icon is IconData || icon is Widget,
  // 'TabItem only support IconData and Widget');
}
